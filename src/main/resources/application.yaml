server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password
    driver-class: org.j2.Driver

  h2:
    console:
      enabled: true

vaadin:
  launch-browser: true